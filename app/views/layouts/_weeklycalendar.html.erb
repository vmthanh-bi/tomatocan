<div class="container-fluid">
  <div class="row justify-content-center pt-2 pb-2" style="position: relative; background-color: #8C1D40; bottom: 20px">
    <% getWeekDays.each_with_index do |date, index|%>
    <button onclick="Panel1(<%=index%>,'#900C3F')" class="btn btn-primary-week" style="width:120px; border-color: white">
      <%=date.strftime("%A")%><br><%= date.month%><%= "/" %><%= date.day %>
    </button>
    <%end%>
  </div>
</div>
<br>
<% getWeekDays.each do |date|%>
  <div class="weekDays border-left border-right border-top">
    <%= render eventPartial, currentDate: date%>
  </div>
<%end%>

<script>
  function Panel1 (panelIndex) {
    var tabButtons=document.querySelectorAll(".weekBtn");
    var tabPanels=document.querySelectorAll(".weekDays");

    tabPanels.forEach(function(node) {
      node.style.display="none";
    });
    tabPanels[panelIndex].style.display="block";
  }
  Panel1(0);
</script>
